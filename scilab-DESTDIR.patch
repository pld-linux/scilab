--- scilab-2.6/Makefile.in	Wed Aug 22 21:32:27 2001
+++ scilab-2.6/Makefile.in.patch	Thu Aug 30 14:55:08 2001
@@ -150,25 +150,25 @@
 LIBPREFIX = @prefix@/lib
 
 install:
-	@if test `pwd` != ${LIBPREFIX}/$(SCIBASE); then \
+	@if test `pwd` != ${DESTDIR}/${LIBPREFIX}/$(SCIBASE); then \
 		touch .binary; \
 		strip $(SCIDIR)/bin/scilex; \
 		(cd tests; make distclean); \
 		(cd examples; make distclean); \
-		(cd .. ; tar cf - $(BINDISTFILES) | (cd ${LIBPREFIX}; tar xf -)); \
-		(cd ${LIBPREFIX}/$(SCIBASE); make); \
+		(cd .. ; tar cf - $(BINDISTFILES) | (cd ${DESTDIR}/${LIBPREFIX}; tar xf -)); \
+		(cd ${DESTDIR}/${LIBPREFIX}/$(SCIBASE); make); \
 		$(RM) .binary; \
 	fi
-	$(RM) /usr/bin/scilab
-	ln -fs ${LIBPREFIX}/$(SCIBASE)/bin/scilab /usr/bin/scilab
-	$(RM) /usr/bin/intersci
-	ln -fs ${LIBPREFIX}/$(SCIBASE)/bin/intersci /usr/bin/intersci
-	$(RM) /usr/bin/intersci-n
-	ln -fs ${LIBPREFIX}/$(SCIBASE)/bin/intersci-n /usr/bin/intersci-n
+	$(RM) ${DESTDIR}/@prefix@/bin/scilab
+	ln -fs ../../..${LIBPREFIX}/$(SCIBASE)/bin/scilab ${DESTDIR}/@prefix@/bin/scilab
+	$(RM) ${DESTDIR}/@prefix@/bin/intersci
+	ln -fs ../../..${LIBPREFIX}/$(SCIBASE)/bin/intersci ${DESTDIR}/@prefix@/bin/intersci
+	$(RM) /@prefix@/bin/intersci-n
+	ln -fs ../../..${LIBPREFIX}/$(SCIBASE)/bin/intersci-n ${DESTDIR}/@prefix@/bin/intersci-n
 
 uninstall:
-	$(RM) -r ${LIBPREFIX}/$(SCIBASE)
-	$(RM) /usr/bin/scilab
-	$(RM) /usr/bin/intersci
-	$(RM) /usr/bin/intersci-n
+	$(RM) -r ${DESTDIR}/${LIBPREFIX}/$(SCIBASE)
+	$(RM) /@prefix@/bin/scilab
+	$(RM) /@prefix@/bin/intersci
+	$(RM) /@prefix@/bin/intersci-n
 
